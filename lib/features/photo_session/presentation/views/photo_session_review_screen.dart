import 'package:flutter/material.dart';

class PhotoSessionReviewScreen extends StatelessWidget {
  const PhotoSessionReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
